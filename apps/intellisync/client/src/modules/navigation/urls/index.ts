import { authUrls } from '@/modules/auth/urls';
import { chatbotsUrls } from '@/modules/chatbots/urls';
import { settingsUrls } from '@/modules/settings/urls';

export const urlPaths = {
  chatbots: chatbotsUrls,
  settings: settingsUrls,
  auth: authUrls,
};
