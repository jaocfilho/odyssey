import { generateEndingCell, generateStartingCell } from "../roadmapCell";

export const generateRoadmapBar = () => {
  const starting = generateStartingCell();
  const ending = generateEndingCell();
};
