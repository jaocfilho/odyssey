export const sizeUtils = {
  wFull: (value: boolean) => ({
    width: "100%"
  }),

  wScreen: (value: boolean) => ({
    width: "100vw"
  }),

  hFull: (value: boolean) => ({
    height: "100%"
  }),

  hScreen: (value: boolean) => ({
    height: "100vh"
  }),
};
